<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp">

    <ImageView
        android:id="@+id/imgDrawerBackground"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:src="@drawable/image_header"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/imgDrawerAvatar"
        android:layout_width="@dimen/imageview_avatar_height"
        android:layout_height="@dimen/imageview_avatar_width"
        android:layout_margin="20dp"
        app:layout_constraintLeft_toLeftOf="@id/imgDrawerBackground"
        app:layout_constraintTop_toTopOf="@id/imgDrawerBackground"
        tools:src="@tools:sample/avatars" />

    <TextView
        android:id="@+id/tvDrawerName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@id/imgDrawerBackground"
        app:layout_constraintLeft_toLeftOf="@id/imgDrawerAvatar"
        tools:text="NguyenHa" />

</androidx.constraintlayout.widget.ConstraintLayout>
